#include<stdio.h>

int main()
{//T1
	/*
	char ch = 0;
	int number = 0;
	while ((ch = getchar()) != 0)
	{
		if (ch == '\n')
			continue;
		if (ch == EOF)
			break;
		number++;
	}
	printf("总共有%d个字符\n", number);
	*/
	
	printf("Done.\n");
	return 0;
}

