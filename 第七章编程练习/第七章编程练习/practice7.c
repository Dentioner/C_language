#include<stdio.h>

int main()
{
/*
	T1
	char ch;
	int kongge = 0;
	int huanhang = 0;
	int all = 0;

	printf("输入一串字符.输入'#'时退出.\n");
	while ((ch = getchar()) != '#')
	{
		if (ch == ' ')
			kongge++;
		else if (ch == '\n')
			huanhang++;
		else
			all++;

	}
	printf("完了.\n");
	printf("共计%d个空格，%d个换行，%d个其他字符", kongge, huanhang, all);
	
	T2
	char ch;
	int times = 0;
	printf("输入一串字符.输入'#'时退出.\n");
	while ((ch = getchar()) != '#')
	{
		printf("%c-%d\t", ch, ch);


		times++;
		
		if ((times%8)==0 && (times != 0))
			printf("\n");
	}
	printf("\nguna.\n");
	
	
	*/


	return 0;
}