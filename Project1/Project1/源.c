#include<stdio.h>
int main()
{
	/*
	int i = -1;
	char input[256];

	printf("Please input the string:");

	do
	{
		i++;
		scanf_s("%c", &input[i]);
	} while (input[i] != '\n');

	for (size_t j = i; j > 0; j--)
	{
		printf("%c", input[j - 1]);
	}
	printf("\n");
	printf("\n%d\n", strlen(input));


	
	float Daphne = 100;
	float Deirdre = 100;
	float d1 = 0.1;
	float d2 = 0.05;
	int year;
	for (year = 0; Daphne >= Deirdre; year++)
	{
		Daphne += 100 * 0.1;
		Deirdre *= 1.05;

	}
	printf("%d years later, Deirdre is richer than Daphne.\n", year);
	printf("Now Daphne has %f dollars, while Deirdre has %f dollars.\n", Daphne, Deirdre);


	下面是范例程序
	double Dap = 100.0;
	double Dei = 100.0;
	int i = 0;

	do
	{
		i++;
		Dap = Dap + 10;
		Dei = Dei * 1.05;
	} while (Dei < Dap);

	printf("After %d year, Deirdre's account:%lf, Daphne's account:%lf\n", i, Dei, Dap);

	int pal = 5;
	int week;
	for (week = 1; pal <= 150; week++)
	{
		pal -= week;
		pal *= 2;

	}
	week -= 1;
	printf("%d weeks later, he has %d friends.\n", week, pal);

	范例程序：
	int Rab = 5;
	int i = 0;
	do
	{
		i++;
		Rab -= i;
		Rab *= 2;
		printf("%d week, Rabnud has %d friends.\n", i, Rab);
	} while (Rab < 150);

*/	
	system("pause");
	return 0;
	}
