#include<stdio.h>
#include<string.h>

#define bool int
#define true 1
#define false 0
#define FLOOR 3
/*
int main()
{
	long int temp_score;
	long int best_score;
	long int best_score_of_upper= 0;
	int floor;
	printf("temp = ");
	scanf("%ld", &temp_score);
	printf("\n");
	printf("best = ");
	scanf("%ld", &best_score);
	printf("\n");
	printf("best of upper = ");
	scanf("%ld", &best_score_of_upper);
	printf("\n");
	if ((temp_score != 0) && (best_score == 0))
	{
		best_score = temp_score;

	}
	if (temp_score > best_score)
	{
		best_score = temp_score;

		//valid_coordinate = verify_coordinate(board, raw, column, chess, opponent_chess);
		//if ((floor == FLOOR)&&valid_coordinate	

	}
	if (best_score < best_score_of_upper)
	{
		printf("cut\n");
	}
	else
	{
		best_score_of_upper = best_score;
	}
	printf("temp = %ld", temp_score);

	printf("\n");
	printf("best = %ld", best_score);

	printf("\n");
	printf("best of upper = %ld", best_score_of_upper);

	printf("\n");

	system("pause");
	return 0;

}

*/

int main()
{
	if (true&&true&&true&&(false||false)&&true)
		printf("bingo\n");
	system("pause");
	return 0;
}

